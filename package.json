{
  "name": "extbackend",
  "version": "1.0.0",
  "main": "backend.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cert": "node bin/generate_cert.js server",

    "start": "npm run build && NODE_ENV=consul node ./build/index.js",
    "start:dev": "nodemon src/index.js --exec babel-node --legacy-watch",
    "clean": "rm -rf build && mkdir build",
    "build-server": "babel -D -d ./build ./src -s",
    "build": "npm run clean && npm run build-server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/craigwuffles/ExtBackEnd.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/craigwuffles/ExtBackEnd/issues"
  },
  "homepage": "https://github.com/craigwuffles/ExtBackEnd#readme",
  "engines": {
    "node": "8.11.3"
  },
  "dependencies": {
    "boom": "^7.2.0",
    "color": "^3.0.0",
    "commander": "^2.15.0",
    "hapi": "^17.2.2",
    "inert": "^5.1.0",
    "jsonwebtoken": "^8.2.0",
    "request": "^2.85.0",
    "selfsigned": "^1.10.2"
  }
}
